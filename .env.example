# 保存サーバーの接続設定。コピーして .env を作り、値を埋める。
#
# PostgreSQL（本番想定）
DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/ikoi

# 検証用にSQLiteを使う場合は上をコメントアウトして↓を使う
# DATABASE_URL=sqlite:///inspections.db

# サーバーの待受ポート（既定 5000）
PORT=5000
